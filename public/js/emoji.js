/**
 * Emoji Module
 *
 * Stub for future emoji picker expansion (Sprint 8).
 * Registers under the window.Chatter namespace.
 */

window.Chatter = window.Chatter || {};

window.Chatter.emoji = {
  initialized: false,

  init() {
    this.initialized = true;
  },
};
